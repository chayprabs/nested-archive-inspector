# Contributing

1. Fork and branch from `cursor/archive-vet-build` or `main`.
2. Run `pnpm install`, `pnpm lint`, `pnpm test`, and worker `pytest`.
3. Use conventional commits (`feat:`, `fix:`, `test:`, `docs:`, `chore:`).
